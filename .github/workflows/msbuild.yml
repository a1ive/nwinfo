name: MSBuild

on:
  workflow_dispatch:
  push:
    branches:
      - "master"

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: .

jobs:
  generate-docs:
    runs-on: ubuntu-latest

    steps:
    - name: Run git checkout
      uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Create PDF
      uses: BaileyJM02/markdown-to-pdf@v1
      with:
        input_path: docs/README.md
        output_dir: build
        images_dir: docs/images
        image_import: ./images
        build_html: false
        table_of_contents: false

    - name: Upload PDF
      uses: actions/upload-artifact@v6
      with:
        name: README.pdf
        path: build/README.pdf

  build:
    runs-on: windows-latest
    needs: generate-docs

    steps:
    - name: Run git checkout
      uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v2

    - name: Restore NuGet packages
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: nuget restore ${{env.SOLUTION_FILE_PATH}}

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      # Add additional options to the MSBuild command line here (like platform or verbosity level).
      # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
      run: |
        msbuild /m /p:Configuration=Release /p:platform=x64 ${{env.SOLUTION_FILE_PATH}}
        msbuild /m /p:Configuration=Release /p:platform=x86 ${{env.SOLUTION_FILE_PATH}}
        msbuild /m /p:Configuration=DLLRelease /p:platform=x64 ${{env.SOLUTION_FILE_PATH}}
        msbuild /m /p:Configuration=DLLRelease /p:platform=x86 ${{env.SOLUTION_FILE_PATH}}

    - name: Download README.pdf
      uses: actions/download-artifact@v6
      with:
        name: README.pdf
        path: .

    - name: Prepare Packages
      shell: pwsh
      run: |
        $TargetFolder = "PKG"
        New-Item -ItemType Directory -Path $TargetFolder -Force | Out-Null
        Copy-Item -Path "x64\Release\nwinfo.exe" -Destination "$($TargetFolder)\nwinfo.exe"
        Copy-Item -Path "x64\Release\gnwinfo.exe" -Destination "$($TargetFolder)\gnwinfo.exe"
        Copy-Item -Path "Win32\Release\nwinfo.exe" -Destination "$($TargetFolder)\nwinfox86.exe"
        Copy-Item -Path "Win32\Release\gnwinfo.exe" -Destination "$($TargetFolder)\gnwinfox86.exe"
        .\copy_res.ps1 -TargetFolder $TargetFolder

    - name: Run Test
      shell: pwsh
      run: |
        .\PKG\nwinfo.exe --format=JSON --output=report.json --cp=UTF8 --human --bin=HEX `
                         --sys --cpu --net --acpi --smbios --disk --display --pci --usb --spd `
                         --battery --uefi --shares --audio --public-ip --product-policy `
                         --gpu --font --device

    - name: Upload Report
      uses: actions/upload-artifact@v6
      with:
        name: report.json
        path: report.json

    - name: Prepare DLL Package
      shell: pwsh
      run: |
        $TargetFolder = "PKG_DLL"
        New-Item -ItemType Directory -Path $TargetFolder -Force | Out-Null
        $dllTargets = @(
          @{ Platform = "x64"; Source = "x64\DLLRelease" },
          @{ Platform = "x86"; Source = "Win32\DLLRelease" }
        )

        foreach ($t in $dllTargets) {
          $destDir = Join-Path $TargetFolder $t.Platform
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          $dllFiles = "libnw.dll","libnw.lib","libnw.pdb"
          foreach ($f in $dllFiles) {
            Copy-Item -Path (Join-Path $t.Source $f) -Destination (Join-Path $destDir $f)
          }
        }

    - name: Create ZIPs
      run: |
        Compress-Archive -Path "PKG\*" -DestinationPath "nwinfo.zip" -Force
        Compress-Archive -Path "PKG_DLL\*" -DestinationPath "dev.zip" -Force

    - name: Generate Release Notes
      env:
        COMMITS_JSON: ${{ toJSON(github.event.commits) }}
      run: |
        $changelogLines = New-Object System.Collections.Generic.List[string]
        if ("${{ github.event_name }}" -eq "push") {
          Write-Host "Generating changelog from push event payload"
          $commits = $env:COMMITS_JSON | ConvertFrom-Json
          if ($null -ne $commits) {
            foreach ($commit in $commits) {
              $shortSha = $commit.id.Substring(0, 7)
              # Take only the first line of the commit message
              $commitMessage = ($commit.message -split "`r?`n")[0]
              $changelogLines.Add("* $commitMessage ($shortSha) by @$($commit.author.name)")
            }
          }
        }
        else {
          Write-Host "Generating changelog for workflow_dispatch event (latest commit)"
          $latest_commit = git log --pretty=format:'* %s (%h) by @%an' -n 1
          $changelogLines.Add($latest_commit)
        }

        $changelogBody = $changelogLines -join "`n"
        if ([string]::IsNullOrWhiteSpace($changelogBody)) {
          $changelogBody = "No new commits in this push. Automated build from commit ${{ github.sha }}."
        }

        $changelogBody | Out-File -FilePath "release_notes.txt" -Encoding utf8

    - name: Delete old release
      continue-on-error: true
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: |
        gh release delete latest --cleanup-tag --yes
      env:
        GH_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

    - name: Release
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: |
        $date_version = (Get-Date -Format 'yyyyMMdd') + ".${{ github.run_number }}"
        gh release create latest "nwinfo.zip" `
          "dev.zip" `
          --prerelease `
          --title "Build $date_version" `
          --notes-file "release_notes.txt"
      env:
        GH_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
